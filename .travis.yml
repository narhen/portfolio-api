language: python
python:
  - "2.7"
install:
  - pip install -r requirements.txt
script: py.test
services:
    - postgresql
before_script:
  - psql -c 'create database test_db;' -U postgres
addons:
  postgresql: "9.4"
